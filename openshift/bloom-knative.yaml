apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: bloom-service
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/min-scale: "0"
    spec:
      containers:
        - image: BLOOM_IMAGE
          ports:
            - containerPort: 8080
